<UserControl x:Class="WpfApp7.View.AView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp7.View"
             mc:Ignorable="d" 
             xmlns:cmd="http://www.galasoft.ch/mvvmlight"
             xmlns:i ="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="AVIew" FontSize="50" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel>
            <Button x:Name="btn" Content="pas" Command="{Binding Pause}"/>
            <Button x:Name="btn2" Content="re" Command="{Binding Resume}"/>
            <Button Content="start" Command="{Binding Start}"/>
            <Button Content="Stop" Command="{Binding Stop}"/>
        </StackPanel>
    </Grid>
</UserControl>
